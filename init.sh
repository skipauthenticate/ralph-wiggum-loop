#!/bin/bash
# init.sh — Project environment setup (optional)
# Ralph reads this file but does NOT execute it automatically.
# Put setup instructions here so Claude knows how to prepare the environment.

# Example:
# source .venv/bin/activate
# pip install -e ".[dev]"
# export DATABASE_URL="sqlite:///data/dev.db"

echo "No project-specific setup configured. Edit init.sh for your project."
